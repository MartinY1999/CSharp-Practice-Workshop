namespace Operations
{
    public static class MathOperations
    {
        public static int Add(int x, int y) => x + y;
        public static double Add(double x, double y, double z) => x + y + z;
        public static decimal Add(decimal x, decimal y, decimal z) => x + y + z;
    }
}
